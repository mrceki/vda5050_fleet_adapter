# FLEET CONFIG =================================================================
# RMF Fleet parameters

rmf_fleet:
  name: "HML"
  limits:
    linear: [0.5, 0.75] # velocity, acceleration
    angular: [0.6, 2.0] # velocity, acceleration
  profile: # Robot profile is modelled as a circle
    footprint: 0.3 # radius in m
    vicinity: 0.5 # radius in m
  reversible: True # whether robots in this fleet can reverse
  battery_system:
    voltage: 12.0 # V
    capacity: 24.0 # Ahr
    charging_current: 5.0 # A
  mechanical_system:
    mass: 20.0 # kg
    moment_of_inertia: 10.0 #kgm^2
    friction_coefficient: 0.22
  ambient_system:
    power: 20.0 # W
  tool_system:
    power: 0.0 # W
  recharge_threshold: 0.10 # Battery level below which robots in this fleet will not operate
  recharge_soc: 1.0 # Battery level to which robots in this fleet should be charged up to during recharging tasks
  publish_fleet_state: 20.0 # Publish frequency for fleet state, ensure that it is same as robot_state_update_frequency
  account_for_battery_drain: True
  task_capabilities: # Specify the types of RMF Tasks that robots in this fleet are capable of performing
    loop: True
    delivery: True
    clean: False
  actions: ["teleop"]
  # finishing_request: [park, charge] #"nothing" # [park, charge, nothing]
  robots:
    # Here the user is expected to append the configuration for each robot in the fleet.
    TB3_1:
      charger: "start"
    # Ceki:
    #   charger: "charging_station"

fleet_manager:
  ip: "127.0.0.1"
  prefix: "uagv/v1/OSRF"
  port: 22011
  user: "some_user"
  password: "some_password"
  robot_state_update_frequency: 10.0

# reference_coordinates:
#   L1:
#     rmf: [[0, 0]
#           ]
#     robot: [[-10, 9.2]
#           ]
reference_coordinates:
  L1:
    rmf: [[8.67, -11.2],
          [11.4, -11.2],
          [11.5, -7.27],
          [8.72, -7.21],
          #[7.16,-9.27]
          ]
    robot: [[-1.35, -1.95],
          [1.35, -2.00],
          [1.45, 1.95],
          [-1.3, 2.0],
          #[-2.85, -0.05]
          ]
